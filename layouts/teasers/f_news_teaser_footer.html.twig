{% if content %}
<footer {{ attributes.addClass('footer-fnews') }}>
    <div class="container">
        <div class="fn-content">
            <div class="fn-bloc">
                <ul class="fn-article">
                    <li>
                        {% if content %}
                            {{ content.teaser_fimage }}
                        {% endif %}
                        <div>
                            <a href="#">
                                {% if content %}
                                <h5 {{ region_attributes.teaser_ftext.addClass('fna-title')}}>
                                    {{ content.teaser_ftext }}
                                </h5>
                                {% endif %}
                            </a>
                            {% if content %}
                            <div {{region_attributes.teaser_fdate.addClass('fna-date')}}>
                                {{ content.teaser_fdate }}
                            </div>
                            {% endif %}
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</footer>
{% endif %}